export default class WritingLog {
    constructor(date, book, words, chapters) {
        this.date = date;
        this.book = book;
        this.words = words;
        this.chapters = chapters;
    }
}